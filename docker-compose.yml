services:
  ipburack:
    build: .
    ports:
      - "${PORT:-3002}:${PORT:-3002}"
    volumes:
      - geo-data:/data
    environment:
      - HOST=0.0.0.0
      - PORT=${PORT:-3002}
      - COUNTRY_DB_PATH=/data/country.mmdb
      - CITY_DB_PATH=/data/city.mmdb
      - UPDATE_INTERVAL_HOURS=24
      - API_KEY=${API_KEY:-}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "sh", "-c", "wget --no-verbose --tries=1 --spider http://localhost:$${PORT:-3002}/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s

volumes:
  geo-data:
